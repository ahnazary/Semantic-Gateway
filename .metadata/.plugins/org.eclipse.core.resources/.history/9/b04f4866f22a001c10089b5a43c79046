import java.io.File;
import java.io.IOException;
import java.text.ParseException;

public class Main {
	
	static final double [][][] TRAINING_DATA = {{{1,1} , {-1}}, 							
												{{1, 5} , {+1}},
												{{5, 1} , {-1}},
												{{5, 5} , {+1}},
												};
	
	public static void main(String[] args) throws IOException, ParseException {
	
		FeatureVector FV = new FeatureVector("input","saref.ttl", "SVM"); // 3rd field should be either "WSVM" or "SVM"
		File file = new File("JSON-LD"); 
		file.delete();
		FV.start();
		System.out.println(FV.getURIs().size());
		
		
		JSONLDGenerator temp = new JSONLDGenerator(FV.getInputAddress(), "JSON-LD", FV.getApprovedURIs());
		
			
//		WriteJSON Sensors = new WriteJSON();
//		Sensors.writeJSONFile();
		
//		WeightedSVM wsvm = new WeightedSVM(TRAINING_DATA);
//		wsvm.displayInfoTables();
//		wsvm.handleCommandLine();


	}			
}
